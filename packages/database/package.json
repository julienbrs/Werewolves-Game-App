{
  "name": "database",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "dependencies": {
    "@prisma/client": "^4.12.0"
  },
  "devDependencies": {
    "prisma": "^4.12.0",
    "tsconfig": "workspace:*"
  },
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push --skip-generate"
  }
}